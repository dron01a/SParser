
# Small Parser. 

## 🇷🇺

Небольшая библиотека(набор заголовков) для работы с XML и HTML. Позволяет находить и извлекать информацию из тегов. Позволяет работать с XML/HTML-текстом и документами. 
Дает возможность представления XML/HTML в качестве древа.

## Пример

```HTML 
<?xml version="1.0" encoding="utf8"?>
<root>
    <note num="one">
        записка 1
    </note>
    <note num="dva">
        записка 2
    </note>
</root>
```

```C++
#include "SParser.h"
#include <iostream>

int main(){
    SP::HTML_tree _t = load_HTML_from_file("test.html");

    std::cout << _t.get_tag("one",{{"num","dva"}}) << std::endl;

    return 0;
}
